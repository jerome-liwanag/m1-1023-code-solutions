/* exported getFirstChar */
// PSEUDOCODE:
// Define a function named getFirstChar that takes in one parameter: string.
// Access the first character of the string parameter through its index.
// Similar to the first element of arrays, the index that represents the first character of a string is 0.
// Return the first character of the string based on the previous statement.

function getFirstChar(string) {
  return string[0];
}
